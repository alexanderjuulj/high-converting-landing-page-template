/**
 * Props for the NavLinks component.
 *
 * @property onClick - Optional callback function invoked when a navigation link is clicked.
 */

export interface NavLinksProps {
  onClick?: () => void;
}
